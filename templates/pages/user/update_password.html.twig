{% extends 'base.html.twig' %}

{% block title %}Password update
{% endblock %}

{% block body %}
	<div>
		<h1 class="h1">Password update</h1>

		{% for message in app.flashes('warning') %}
			<div class="alert alert-warning">
				{{ message }}
			</div>
		{% endfor %}
		{{ form(form) }}
		</div>
	{% endblock %}
